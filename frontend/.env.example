# Connect to database for website and database
DATABASE_URL=postgresql://postgres:password@localhost:5432/stinkbot
NODE_ENV=development

# Required for signing per-game moderator dashboard sessions. Generate with:
# openssl rand -base64 32
GAME_TOKEN_SECRET=replace-with-a-random-secret
